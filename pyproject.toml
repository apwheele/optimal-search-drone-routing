[project]
name = "optimal-search-drone-routing"
version = "0.1.0"
description = "Coordinated probability-weighted search paths for multiple drones"
requires-python = ">=3.11"
dependencies = [
    "ipykernel>=6.29",
    "jupyter>=1.1",
    "matplotlib>=3.9",
    "nbconvert>=7.16",
    "nbformat>=5.10",
    "numpy>=2.0",
    "ortools>=9.12",
    "pandas>=2.2",
    "psutil>=7.0",
    "pytest>=8.3",
]

[tool.uv]
package = false
